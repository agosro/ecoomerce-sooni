# Backend Environment Variables

# Base URL del Frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# Puerto del servidor
PORT=4000

# MongoDB
MONGO_URI=mongodb://localhost:27017/sooni-skincare

# JWT
JWT_SECRET=tu_clave_secreta_super_segura_aqui

# Google OAuth (opcional, para login con Google)
GOOGLE_CLIENT_ID=tu_google_client_id
GOOGLE_CLIENT_SECRET=tu_google_client_secret

# MercadoPago (para pagos)
MERCADOPAGO_ACCESS_TOKEN=tu_token_mercadopago

# URL pública del backend (para callbacks de MercadoPago)
BACKEND_URL=http://localhost:4000

# Credenciales del admin inicial (seed — NO subir valores reales a git)
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=CambiaEstaPasswordSegura123!
ADMIN_NAME=Admin SOONI

# Credenciales del usuario viewer (solo lectura del dashboard)
VIEWER_EMAIL=viewer@tudominio.com
VIEWER_PASSWORD=CambiaEstaPasswordSegura456!
VIEWER_NAME=Viewer SOONI

# Credenciales del usuario demo (carrito y compras, sin editar perfil)
DEMO_EMAIL=demo@tudominio.com
DEMO_PASSWORD=DemoPassword789!
DEMO_NAME=Demo User
